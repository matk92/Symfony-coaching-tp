{% extends 'base.html.twig' %}

{% block title %}Modifier Coach{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Modifier Coach</h1>

        {{ include('coach/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a class="btn btn-secondary mt-3" href="{{ path('app_coach_index') }}">Retour à la liste</a>

        {{ include('coach/_delete_form.html.twig') }}
    </div>
{% endblock %}